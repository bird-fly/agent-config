## Skill 使用总则
- 先理解任务意图，再主动查找可用 skill；不要只按技术栈或记忆硬套 skill 名。
- 优先选择流程类 skill，再进入具体实现；若没有合适 skill，就按项目规则直接处理。
- 遇到 bug、失败、不生效、性能异常、规划、重构、代码审查、新功能、文档/文件处理等任务时，先搜索或检查是否有对应 skill。
- 若多个 skill 都可能适用，选择最小必要组合，并说明使用顺序。
- 若用户明确要求简单/快速/小改，保持轻量流程，但仍要检查是否存在明显匹配的 skill。
